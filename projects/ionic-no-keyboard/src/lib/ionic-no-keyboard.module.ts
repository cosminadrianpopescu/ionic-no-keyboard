import { NgModule } from '@angular/core';
import {NoKeyboard} from './ionic-no-keyboard.directive';

@NgModule({
  declarations: [NoKeyboard],
  imports: [
  ],
  exports: [NoKeyboard]
})
export class NoKeyboardModule { }
